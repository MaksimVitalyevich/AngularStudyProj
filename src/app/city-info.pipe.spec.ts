import { CityInfoPipe } from './city-info.pipe';

describe('CityInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new CityInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
